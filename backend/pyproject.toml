[project]
name = "gaussian-splats-backend"
version = "0.1.0"
description = "Local-first MP4 to Gaussian Splat processing API"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
asyncio_mode = "auto"

